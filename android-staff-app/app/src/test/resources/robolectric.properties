# Unit tests construct collaborators directly (no Hilt graph), so use a stub
# Application instead of StaffApp, whose onCreate requires injected dependencies.
application=android.app.Application
sdk=34
